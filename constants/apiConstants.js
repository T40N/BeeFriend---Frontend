const base = "https://df84-31-183-147-37.eu.ngrok.io";

const user = {
  register: `${base}/auth/signup`,
  login: `${base}/auth/login`,
};

const apiConstants = {
  base,
  user,
};

export default apiConstants;
